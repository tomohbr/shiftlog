# @squareetlabs/capacitor-subscriptions（StoreKit 2 のプラグイン）は
# peerDependencies に @capacitor/core ^6 || ^7 を宣言しているが、本体は Capacitor 8 を使う。
# ネイティブ側は Capacitor のプラグインAPIに乗っているだけで 8 でも動くため、
# ここで解決方式を緩めておく。これが無いと npm 7 以降は ERESOLVE で install に失敗し、
# Railway のビルドがそのまま落ちる。
legacy-peer-deps=true
